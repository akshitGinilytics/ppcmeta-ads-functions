/// <reference types="@google-cloud/firestore" />
interface Options {
    firestore: FirebaseFirestore.Firestore;
}
export declare class BigBatch {
    private firestore;
    private currentBatch;
    private batchArray;
    private operationCounter;
    constructor({ firestore }: Options);
    set(ref: FirebaseFirestore.DocumentReference, data: object, options?: FirebaseFirestore.SetOptions): void;
    update(ref: FirebaseFirestore.DocumentReference, data: object): void;
    delete(ref: FirebaseFirestore.DocumentReference): void;
    commit(): Promise<FirebaseFirestore.WriteResult[][]>;
}
export {};
