import User from "./User.js";
import { UserCustomer } from "./Customer.js";
import { UserCampaign } from "./Campaign.js";
import DataLoader from "dataloader";
export default class UserDataLoaders {
    private userId;
    userCustomersCampaigns: {
        [customerId: string]: UserCampaign[];
    };
    user: DataLoader<string, User | null>;
    userCampaign: DataLoader<string, UserCampaign | null>;
    userCustomer: DataLoader<string, UserCustomer | null>;
    userCustomersIds: string[];
    constructor(userId: string);
    setUserId(userId: string): void;
    batchUsers(userIds: Readonly<string[]>): Promise<(User | null)[]>;
    batchUserCustomers(customersIds: Readonly<string[]>): Promise<(UserCustomer | null)[]>;
    batchUserCampaigns(campaignIds: Readonly<string[]>): Promise<(UserCampaign | null)[]>;
    getUserCustomers(): Promise<(Error | UserCustomer | null)[]>;
    getUserCustomerCampaigns(customerId: string): Promise<UserCampaign[]>;
}
