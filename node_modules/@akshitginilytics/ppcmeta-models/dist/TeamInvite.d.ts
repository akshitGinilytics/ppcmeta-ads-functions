import { InviteDataModel, Role } from "@GeneralTypes";
import { Timestamp } from "firebase-admin/firestore";
import { Teammate } from "@GraphqlTypes";
export default class TeamInvite implements InviteDataModel {
    readonly inviteId: string;
    invitedBy: {
        userId: string;
        displayName: string;
        email: string;
    };
    email: string;
    role: Role;
    status: string;
    teamId: string;
    teamName: string;
    updated: Timestamp;
    created: Timestamp;
    constructor(InviteId: string);
    static createInvite(email: string, role: Role, teamId: string, teamName: string, sender: Teammate): Promise<TeamInvite>;
    setInviteStatus(status: string): void;
    getInvite(): Promise<void>;
    acceptInvite(): Promise<void>;
    saveInvite(): Promise<void>;
    declineInvite(): Promise<void>;
    toJson(): {
        inviteId: string;
        invitedBy: {
            userId: string;
            displayName: string;
            email: string;
        };
        email: string;
        role: Role;
        status: string;
        teamId: string;
        teamName: string;
        updated: Timestamp;
        created: Timestamp;
    };
    setAttributes(attributes: Partial<InviteDataModel>): void;
}
